
# Use Node 22 Alpine (Lightweight and secure)
FROM node:22-alpine

# Set working directory
WORKDIR /app

# Install build tools required for bcrypt and other native modules
# (Python, Make, and G++ are needed for node-gyp)
RUN apk add --no-cache python3 make g++

# Copy package files first to leverage caching
COPY package*.json ./

# Install dependencies (npm ci is faster/cleaner for CI/CD/Docker)
RUN npm ci

# Copy the rest of the source code
COPY . .

# Expose the port your app runs on
# (Make sure this matches the PORT in your index.js or .env)
EXPOSE 3000

# Start the application
CMD ["npm", "start"]

